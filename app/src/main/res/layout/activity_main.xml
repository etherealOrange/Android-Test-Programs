<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F0F0F0"
    tools:context=".MainActivity">

    <ImageView
        android:layout_width="120dp"
        android:layout_height="200dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:id="@+id/nomarl_imageView"
        android:layout_margin="50dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_launcher_background"
        />
    <com.google.android.material.button.MaterialButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/change_image_button"
        android:text="保存图片"
        android:textSize="20sp"
        app:layout_constraintTop_toBottomOf="@+id/nomarl_imageView"
        app:layout_constraintStart_toStartOf="@+id/nomarl_imageView"
        />
    <com.google.android.material.button.MaterialButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="显示图片"
        android:id="@+id/show_BTN"
        android:textSize="20sp"
        app:layout_constraintTop_toBottomOf="@+id/nomarl_imageView"
        app:layout_constraintStart_toEndOf="@+id/change_image_button"
        />
    <com.google.android.material.button.MaterialButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/Notify_BTN"
        android:text="发布通知"
        android:textSize="20sp"
        app:layout_constraintTop_toBottomOf="@+id/change_image_button"
        app:layout_constraintStart_toStartOf="@+id/change_image_button"
        />


</androidx.constraintlayout.widget.ConstraintLayout>